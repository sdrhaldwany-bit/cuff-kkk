-- OMEGA-RED // MESSAGE SCRIPT

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local PlayerGui = LP:WaitForChild("PlayerGui")

-- إنشاء واجهة الرسالة
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MessageGUI"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = PlayerGui

-- الخلفية
local background = Instance.new("Frame")
background.Size = UDim2.new(0, 400, 0, 200)
background.Position = UDim2.new(0.5, -200, 0.5, -100)
background.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
background.BackgroundTransparency = 0.1
background.BorderSizePixel = 0
local bgCorner = Instance.new("UICorner")
bgCorner.CornerRadius = UDim.new(0, 15)
bgCorner.Parent = background
background.Parent = screenGui

-- عنوان
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 45)
title.Position = UDim2.new(0, 0, 0, 0)
title.Text = "⚠️ تنبيه ⚠️"
title.TextScaled = true
title.TextColor3 = Color3.fromRGB(255, 50, 50)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.Parent = background

-- خط فاصل
local line = Instance.new("Frame")
line.Size = UDim2.new(0.9, 0, 0, 2)
line.Position = UDim2.new(0.05, 0, 0, 45)
line.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
line.BorderSizePixel = 0
line.Parent = background

-- الرسالة الرئيسية
local message = Instance.new("TextLabel")
message.Size = UDim2.new(0.9, 0, 0, 60)
message.Position = UDim2.new(0.05, 0, 0, 55)
message.Text = "عذراً عزيزي، السكربت تغير كامل"
message.TextScaled = true
message.TextColor3 = Color3.fromRGB(255, 255, 255)
message.BackgroundTransparency = 1
message.Font = Enum.Font.Gotham
message.Parent = background

-- رابط التيك توك
local tiktok = Instance.new("TextLabel")
tiktok.Size = UDim2.new(0.9, 0, 0, 40)
tiktok.Position = UDim2.new(0.05, 0, 0, 120)
tiktok.Text = "📱 تيك توك: @hacker_20111"
tiktok.TextScaled = true
tiktok.TextColor3 = Color3.fromRGB(100, 200, 255)
tiktok.BackgroundTransparency = 1
tiktok.Font = Enum.Font.GothamBold
tiktok.Parent = background

-- نص تعليمات
local instruction = Instance.new("TextLabel")
instruction.Size = UDim2.new(0.9, 0, 0, 25)
instruction.Position = UDim2.new(0.05, 0, 0, 165)
instruction.Text = "ارسلي عشان اعطيك السكربت الجديد"
instruction.TextScaled = true
instruction.TextColor3 = Color3.fromRGB(200, 200, 200)
instruction.BackgroundTransparency = 1
instruction.Font = Enum.Font.Gotham
instruction.Parent = background

-- زر إغلاق
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -35, 0, 5)
closeBtn.Text = "✖"
closeBtn.TextScaled = true
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 90)
closeBtn.BorderSizePixel = 0
local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 8)
closeCorner.Parent = closeBtn
closeBtn.Parent = background

closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- إمكانية السحب
local dragging = false
local dragStart = nil
local startPos = nil

background.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = background.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        background.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

print("✅ تم تشغيل السكربت | تواصل مع @hacker_20111")
